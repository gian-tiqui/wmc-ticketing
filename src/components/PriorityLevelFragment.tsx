import React from "react";
import { Ticket } from "../types/types";

interface Props {
  ticket: Ticket;
}

const PriorityLevelFragment: React.FC<Props> = ({ ticket }) => {
  return <form></form>;
};

export default PriorityLevelFragment;
